# Context: refactor-agnostic

Task context and supporting artifacts for making `pi-multicodex` platform-agnostic.

## Objective
Restructure multicodex so account storage, token refresh, selection/rotation, and usage tracking are reusable outside pi extension runtime (e.g., Mnemos bridge/gateway).

## Artifacts

- `context/restructure-scope-and-target-architecture.md`
  - Scope boundaries, target package/module split, and migration strategy

- `progress.md`
  - Execution checklist and progress updates
